# SQL Scripts

This directory stores PostgreSQL scripts, including:

- Table creation scripts
- Initial data scripts
- Database migration scripts

## Stage 1

Run `stage1.sql` against `water_leakage_system` after pulling the first-stage implementation. The script is idempotent and adds button permissions and supporting unique indexes.
