server:
  port: 8080
  servlet:
    context-path: /api

spring:
  application:
    name: water-leakage-backend

  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://192.168.4.20:5432/water_leakage_system
    username: postgres
    password: wzk123

mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true
  global-config:
    db-config:
      id-type: auto
      logic-delete-field: deleted
      logic-delete-value: 1
      logic-not-delete-value: 0

logging:
  level:
    com.donghai.leakage: debug